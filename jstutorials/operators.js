var a=500
        var b="Code With Harry"
        console.log(b);
        console.log(a);
        console.log("Hello World");
        var c = 345.65

        // Operators in JS
        // binary operator and unary operator
        // 1. unary operator - it has single operand (x = -x)
        // Example of Unary
        console.log(c);
        c = -c;
        console.log(c);
        
        // 2. Binary operator - it has 2 operands (x = x+6)
        // Example of binary
        var d = 500+350;
        console.log(d);
        // operands in JS - entities that operators operate on 
        